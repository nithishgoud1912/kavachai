"""
KavachAI Backend — Shared FastAPI Dependencies
Provides: DB session, session auth (Phase 2), and common dependency injection.
"""

# Dependencies will be added as phases are built:
# Phase 2: get_current_session (session auth)
# Phase 1: get_db (SQLAlchemy async session)
